using UnityEngine;
using UnityEditor;

public static class CoinManager
{
    //When coming to next level, update this class and then load level accordingly.
    public static int numOfCoins { get; set; }
}